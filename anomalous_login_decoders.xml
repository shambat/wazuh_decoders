<decoders>

  <decoder name="ssh_login_unusual_ip">
    <prematch>Accepted password</prematch>
    <regex>Accepted passwords for (\S+) from (\S+)</regex>
    <order>user srcip</order>
  </decoder>

</decoders>
